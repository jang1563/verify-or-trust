# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.ipynb_checkpoints/

# data artifacts that must be regenerated from source (license: not redistributed)
data/raw/
data/*.h5ad
*.h5ad
data/cache/
data/substrates/state_tahoe*.csv   # Tahoe substrate: regenerate from HF (Arc non-commercial; ~1.3 GB)

# local research scratch (not part of the released package)
research/

# secrets — never commit
*.key
*_api_key.txt
.env

# local episode outputs (large; regenerate)
runs/
*.jsonl
!tests/**/*.jsonl

# OS
.DS_Store
